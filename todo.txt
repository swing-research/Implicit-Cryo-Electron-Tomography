- remove useless function in the utils script (reconstruction)
- handle the __main__ in utils_sampling.py
- define other model for implicit net or remove it
- multi shrec XP
   Davinci (running)
   python -m experiment_scripts.simulation_all_shrec_models --no_aretomo --no_comparison
   DGX:
   python -m experiment_scripts.simulation_all_shrec_models --no_gen_data --no_train --no_comparison
   scp + Davinci
   python -m experiment_scripts.simulation_all_shrec_models --no_gen_data --no_train --no_aretomo
   TODO: compare results inot the one bar plot
- real data XP
- different SNR
   Davinci (running)
   python -m experiment_scripts.simulation_SNR_influence --no_aretomo --no_comparison
   DGX:
   python -m experiment_scripts.simulation_SNR_influence --no_gen_data --no_train --no_comparison
   scp + Davinci
   python -m experiment_scripts.simulation_SNR_influence --no_gen_data --no_train --no_aretomo
   TODO: compare results inot the one line plot
- Timing (re run to tr the new command max_memory)
   python -m experiment_scripts.simulation_shrec_timing 
- FSC per epoch



@Vinith
-etomo on shrec 0
